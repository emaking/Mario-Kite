	;_____________________________SPRITES
;	For mpp.Mpa=Each Mpa
;		UpdateMpa( mpp )

;	Next
	;For nn.Ng=Each Ng
	;	UpdateNg( nn )
	;Next
;	For pp.Pa=Each Pa
;		UpdatePa( pp )
;	Next
	For ss.Sh=Each Sh
		UpdateShark( ss )
	Next
;	For xx.Nx=Each Nx
;		UpdateNx( xx )
;	Next
;	For tt.Tr=Each Tr
;		UpdateTree( tt )
;	Next
	
	
	;For rr.Ra=Each Ra
	;	UpdateRa( rr )
	;Next